# new_pk_draw 工作日誌

> 紅白對抗賽 重製版（舊版 pk_draw 因 Firebase 安全規則到期而廢棄）

---

## 目前確認正常的功能（✅ = 不准動）

✅ PDF 上傳（含中文檔名轉換，toSafeKey() 處理）
✅ 開始新回合（ELO 最近配對邏輯）
✅ 即時票數顯示（3 秒輪詢）
✅ 結算本回合（ELO 更新）
✅ 學生端投票 + 防重複（localStorage token）
✅ Lightbox 點圖放大（index.html）

---

## 架構決策紀錄

### 為什麼重製？
- 舊版 Firebase 測試模式安全規則 30 天到期，Firestore 全面拒絕存取
- 舊版 Vercel 架構根本限制：PDF 切割 + 學生投票 兩個功能無法在同一 Serverless 環境共存

### 新架構：純前端 + Supabase
- **棄用**：Python FastAPI 後端、Firebase、Cloudinary、Vercel Function
- **改用**：純 HTML/JS 前端 + Supabase（DB + Storage + Realtime）
- **PDF 處理**：老師上傳單頁 PDF → 瀏覽器用 PDF.js 轉圖 → 直接存 Supabase Storage
- **部署**：index.html（學生端）放 GitHub Pages；admin.html（老師端）本地開啟即可

### Supabase 專案資訊
- **Project URL**：https://cnizzgyievmsqdglftrh.supabase.co
- **Publishable key**：sb_publishable_8_fQGxMNZfoypvHQQGsL6w_cqhvvNXz
- **Region**：Northeast Asia (Seoul / ap-northeast-2)
- **Organization**：oskens（Free plan）

### 資料庫結構
- `works`：作品清單（id, image_url, filename, elo, match_count, win_count）
- `system_state`：當前對戰狀態（status, match_id, match_a, match_b, round_number, round_ends_at）
- `votes`：投票紀錄，append-only（match_id, choice A/B, voter_token）

### Storage
- Bucket：`works-images`（public）
- Policy：SELECT/INSERT/UPDATE/DELETE 全開，條件 `bucket_id = 'works-images'`

---

## 2026/04/30 工作記錄

### 完成事項
- 分析舊版失敗原因，確立新架構方向
- Supabase 帳號建立（GitHub OAuth 登入）
- Supabase 專案 `pk-draw` 建立（首爾，免費）
- 資料庫三張表 + RLS 安全規則建立完成
- Storage bucket `works-images` 建立（public）
- Storage 四種操作 policy 建立完成

### 待辦
- [ ] 建立 new_pk_draw 資料夾結構（config.js、admin.html、index.html）
- [ ] 寫 config.js（Supabase 金鑰設定）
- [ ] 寫 admin.html（老師端：批次上傳 PDF、回合控制、即時票數）
- [ ] 寫 index.html（學生端：看兩張作品、投票）
- [ ] 部署 index.html 到 GitHub Pages
- [ ] 完整流程測試

### 未回答的問題（下次開工前確認）
- 計時器行為：固定秒數倒數自動結算，還是老師手動按結束？
- 圖片顯示：要不要顯示班級座號？還是匿名？

### 已確認的設計決策
- ✅ 老師**手動**按「結算本回合」
- ✅ 學生投票頁**顯示班級座號**（即 PDF 檔名去掉副檔名）

---

## 2026/05/01 工作記錄

### 完成事項
- 修正 Supabase RLS 寫入權限（`works` / `system_state` 從 service_role 改為 anon 可寫）
- 建立 `config.js`（Supabase 金鑰）
- 建立 `admin.html`（老師端完整功能）
  - 批次拖曳上傳 PDF，PDF.js 在瀏覽器轉 JPG，直傳 Supabase Storage
  - 開始新回合（自動挑場數少的兩件作品）
  - 結算回合（計票、更新 ELO、恢復等待狀態）
  - 3 秒輪詢即時票數
  - ELO 排行榜 + 縮圖總覽
  - 一鍵重設全部資料
- 建立 `index.html`（學生端）
  - 3 秒輪詢比對狀態
  - 顯示兩件作品圖片 + 班級座號
  - 投票按鈕 + localStorage 防重複投票
  - 回合結束過渡動畫

### 待辦
- [ ] 部署 index.html 到 GitHub Pages
- [ ] 產生學生用 QR Code
- [ ] 正式使用前重設資料、重新上傳全班 PDF（scale 已改為 1.0）
- [ ] 完整真機測試（手機掃 QR Code → 投票 → 老師結算）

---

## 2026/05/01 工作記錄（第三段）

### 完成事項
- GitHub repo `oskens1/new_pk_draw` 建立（public）
- `index.html` commit 並部署至 GitHub Pages
- 學生端網址上線：https://oskens1.github.io/new_pk_draw/ ✅（實測正常）
- `admin.html` 新增 QR Code 側邊抽屜（右側標籤點一下滑出，顯示學生端 QR Code，點外側自動收起）

### 待辦
- [ ] 開 admin.html → 重設全部資料（清掉測試資料）
- [ ] 重新上傳全班 PDF（scale 1.0 版）
- [ ] 手機真機測試（掃 QR Code → 投票 → 老師結算完整流程）

### 未來方向備註（使用者確認）
- 【目標】admin.html 也上網，不再依賴本地開啟
- 【目標】建立個人工具集首頁（oskens1.github.io），統一管理所有網頁工具，方便用任何電腦（含公用電腦）辦活動
- 【做法】admin.html 加 JS 密碼保護後推上 GitHub Pages；工具集首頁另建
- 優先度：先跑完這學期活動，之後再做

---

## 2026/05/01 工作記錄（第二段）

### 完成事項
- **本地測試環境建立**：解決筆電 H: 路徑無法跑 HTTP Server 的問題，改用 Anaconda Prompt 指定完整路徑 `C:\Users\osken\anaconda3\python.exe -m http.server 3000`，並將檔案暫時複製到桌面 `pk_test` 資料夾
- **Bug 修復①：中文檔名**：Supabase Storage 不接受中文 key，新增 `toSafeKey()` 函式自動去除非 ASCII 字元，Storage key 用純數字、filename 保留中文顯示
- **Bug 修復②：ELO 配對邏輯**：原本從場數少的 pool 中隨機選，改為「場數最少的當錨點，找 ELO 最接近的對手」，排名收斂更快
- **新功能：Lightbox 點圖放大**：index.html 加入全螢幕 lightbox，學生點圖可放大查看細節，點任意處關閉
- **圖片 scale 降低**：admin.html 的 PDF 轉圖 scale 從 2.0 改為 1.0（每張圖 201KB→69KB，省 66% 流量，實測清晰度足夠）
- **模擬測試**：透過 JS console 模擬 20+ 回合投票，驗證 ELO 配對與結算邏輯正常

### 踩坑記錄
- **筆電 H: 路徑問題**：Google Drive「其他電腦」虛擬路徑 Python http.server 無法正常啟動（靜默退出無錯誤訊息），需複製檔案到本機路徑再啟動
- **Windows Store Python stub**：`where python` 會看到兩個 Python，第一個是 WindowsApps 假 stub，什麼都不執行。筆電要用 `C:\Users\osken\anaconda3\python.exe` 完整路徑
- **Supabase 中文 key**：Storage 物件名稱不支援中文，上傳前必須 sanitize

### 待辦
- [ ] 部署 index.html 到 GitHub Pages（建議在桌電執行）
- [ ] 產生學生用 QR Code
- [ ] 正式使用前：重設全部資料 → 重新上傳全班 PDF（scale 1.0 版）
- [ ] 完整真機測試（手機掃 QR Code → 投票 → 老師結算）

---

## 2026/05/01 工作記錄（第四段）

### 完成事項
- 重設全部測試資料（admin.html 一鍵重設）
- 重新上傳全班 PDF（確認 scale 1.0 為程式預設，之後上傳原始 PDF 即可，無需特別處理）
- 壓力測試通過：Python 腳本模擬 50 人同時投票 × 10 回合，全數成功，真機同時投票也穩定
- 新增「⚡ 結算並開始新回合」藍色按鈕（連續多回合不需點兩個按鈕）
- admin.html 加入登入密碼保護（SHA-256 雜湊，sessionStorage 記憶本次登入）
- admin.html push 上 GitHub Pages，網路版正式上線：https://oskens1.github.io/new_pk_draw/admin.html ✅
- 建立 `git_push.bat`：一鍵 git add + commit（自動帶日期時間）+ push，不需手動開 CMD

### 待辦
- [ ] 個人工具集首頁（oskens1.github.io），統一管理所有網頁工具
- [ ] 用其他電腦（非開發機）完整測試網路版 admin.html 流程

### 踩坑記錄
- **git 重新初始化**：new_pk_draw 資料夾沒有 .git，需重新 `git init` + `git remote add` + `git push --force`；分支名稱預設為 master，需 `git branch -M main` 改名後才能推到 remote 的 main
- **bash 沙箱三大限制**（已整理進工作須知提案）：① 無 GitHub 認證不能 git push；② 看不到 Google Drive 虛擬路徑；③ 無法執行 Windows CMD/PowerShell
